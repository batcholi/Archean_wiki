# Nomi di oggetti e moduli

Tutti gli oggetti, i blocchi e i componenti del gioco, con il nome esatto (`<Module>.<Item>`) da usare nei tuoi programmi XenonCode.

> Suggerimento: ottenibili anche a runtime con `get_recipes()` e `get_recipe()`.

## ARCHEAN_aileron

- `ARCHEAN_aileron.Aileron` — **Aileron** *(pila max: 256)*

## ARCHEAN_anchor

- `ARCHEAN_anchor.GroundAnchor` — **Ground Anchor** *(pila max: 256)*

## ARCHEAN_avatar

- `ARCHEAN_avatar.Canister` — **Canister** *(pila max: 256)*
- `ARCHEAN_avatar.PilotSeat` — **Pilot Seat** *(pila max: 64)*
- `ARCHEAN_avatar.Bobblehead` — **Bobblehead** *(pila max: 256)*

## ARCHEAN_battery

- `ARCHEAN_battery.LowVoltageBattery` — **Low Voltage Battery** *(pila max: 256)*
- `ARCHEAN_battery.HighVoltageBattery` — **High Voltage Battery** *(pila max: 64)*

## ARCHEAN_beacon

- `ARCHEAN_beacon.Beacon` — **Beacon** *(pila max: 256)*

## ARCHEAN_beep

- `ARCHEAN_beep.Buzzer` — **Buzzer** *(pila max: 256)*

## ARCHEAN_build

- `ARCHEAN_build.SteelBeam` — **Steel Beam** *(pila max: 10000)*
- `ARCHEAN_build.SteelRod` — **Steel Rod** *(pila max: 10000)*
- `ARCHEAN_build.ConcreteBlock` — **Concrete Block** *(pila max: 10000)*
- `ARCHEAN_build.CompositeBlock` — **Composite Block** *(pila max: 10000)*
- `ARCHEAN_build.SteelBlock` — **Steel Block** *(pila max: 10000)*
- `ARCHEAN_build.AluminiumBlock` — **Aluminium Block** *(pila max: 10000)*
- `ARCHEAN_build.GlassBlock` — **Glass Block** *(pila max: 10000)*
- `ARCHEAN_build.LeadBlock` — **Lead Block** *(pila max: 256)*
- `ARCHEAN_build.ConstructorTool` — **Constructor Tool** *(pila max: 1)*
- `ARCHEAN_build.DestructorTool` — **Destructor Tool** *(pila max: 1)*
- `ARCHEAN_build.BlueprintTool` — **Blueprint Tool** *(pila max: 1)*
- `ARCHEAN_build.PaintTool` — **Paint Tool** *(pila max: 1)*
- `ARCHEAN_build.LabelTool` — **Label Tool** *(pila max: 1)*
- `ARCHEAN_build.DashboardTool` — **Dashboard Tool** *(pila max: 1)*
- `ARCHEAN_build.SpoolTool` — **Spool Tool** *(pila max: 1)*
- `ARCHEAN_build.DataCable` — **Data Cable** *(pila max: 10000000)*
- `ARCHEAN_build.LowVoltageCable` — **Low Voltage Cable** *(pila max: 10000000)*
- `ARCHEAN_build.HighVoltageCable` — **High Voltage Cable** *(pila max: 10000000)*
- `ARCHEAN_build.FluidPipe` — **Fluid Pipe** *(pila max: 10000000)*
- `ARCHEAN_build.ItemConduit` — **Item Conduit** *(pila max: 10000000)*
- `ARCHEAN_build.SmallPivot` — **Small Pivot** *(pila max: 256)*
- `ARCHEAN_build.SmallHinge` — **Small Hinge** *(pila max: 256)*
- `ARCHEAN_build.LinearTrack` — **Linear Track** *(pila max: 256)*
- `ARCHEAN_build.DockableDoor` — **Dockable Door** *(pila max: 8)*
- `ARCHEAN_build.DockingPort` — **Docking Port** *(pila max: 64)*
- `ARCHEAN_build.Volume` — **Volume** *(pila max: 256)*
- `ARCHEAN_build.Hitch` — **Hitch** *(pila max: 64)*

## ARCHEAN_celestial

- `ARCHEAN_celestial.Aluminium` — **Aluminium** *(pila max: 10000000)*
- `ARCHEAN_celestial.Chrome` — **Chrome** *(pila max: 10000000)*
- `ARCHEAN_celestial.Copper` — **Copper** *(pila max: 10000000)*
- `ARCHEAN_celestial.Gold` — **Gold** *(pila max: 10000000)*
- `ARCHEAN_celestial.Iron` — **Iron** *(pila max: 10000000)*
- `ARCHEAN_celestial.Lead` — **Lead** *(pila max: 10000000)*
- `ARCHEAN_celestial.Nickel` — **Nickel** *(pila max: 10000000)*
- `ARCHEAN_celestial.Silicon` — **Silicon** *(pila max: 10000000)*
- `ARCHEAN_celestial.Silver` — **Silver** *(pila max: 10000000)*
- `ARCHEAN_celestial.Tin` — **Tin** *(pila max: 10000000)*
- `ARCHEAN_celestial.Titanium` — **Titanium** *(pila max: 10000000)*
- `ARCHEAN_celestial.Tungsten` — **Tungsten** *(pila max: 10000000)*
- `ARCHEAN_celestial.Uranium` — **Uranium** *(pila max: 10000000)*
- `ARCHEAN_celestial.Carbon` — **Carbon** *(pila max: 10000000)*
- `ARCHEAN_celestial.Sulfur` — **Sulfur** *(pila max: 10000000)*
- `ARCHEAN_celestial.Fluorite` — **Fluorite** *(pila max: 10000000)*
- `ARCHEAN_celestial.Rock` — **Rock** *(pila max: 10000)*
- `ARCHEAN_celestial.RockRakeTool` — **Rock Rake Tool** *(pila max: 1)*
- `ARCHEAN_celestial.OreScannerTool` — **Ore Scanner Tool** *(pila max: 1)*
- `ARCHEAN_celestial.NavInstrument` — **Nav Instrument** *(pila max: 256)*
- `ARCHEAN_celestial.MiniNavInstrument` — **Mini Nav Instrument** *(pila max: 256)*
- `ARCHEAN_celestial.SolarPanel` — **Solar Panel** *(pila max: 256)*
- `ARCHEAN_celestial.SolarSensor` — **Solar Sensor** *(pila max: 256)*
- `ARCHEAN_celestial.MiningDrill` — **Mining Drill** *(pila max: 8)*
- `ARCHEAN_celestial.OreScanner` — **Ore Scanner** *(pila max: 256)*
- `ARCHEAN_celestial.TerrainScanner` — **Terrain Scanner** *(pila max: 256)*
- `ARCHEAN_celestial.GeothermalExchanger` — **Geothermal Exchanger** *(pila max: 64)*

## ARCHEAN_chemical

- `ARCHEAN_chemical.FluidPort` — **Fluid Port** *(pila max: 256)*
- `ARCHEAN_chemical.Electrolyser` — **Electrolyser** *(pila max: 8)*
- `ARCHEAN_chemical.SabatierReactor` — **Sabatier Reactor** *(pila max: 64)*
- `ARCHEAN_chemical.EnrichmentCentrifuge` — **Enrichment Centrifuge** *(pila max: 64)*
- `ARCHEAN_chemical.ChemicalFurnace` — **Chemical Furnace** *(pila max: 8)*

## ARCHEAN_computer

- `ARCHEAN_computer.Computer` — **Computer** *(pila max: 256)*
- `ARCHEAN_computer.MiniComputer` — **Mini Computer** *(pila max: 256)*
- `ARCHEAN_computer.Router` — **Router** *(pila max: 256)*
- `ARCHEAN_computer.MiniRouter` — **Mini Router** *(pila max: 256)*
- `ARCHEAN_computer.HDD` — **HDD** *(pila max: 256)*

## ARCHEAN_gyroscope

- `ARCHEAN_gyroscope.SmallGyroscope` — **Small Gyroscope** *(pila max: 64)*
- `ARCHEAN_gyroscope.Gyroscope` — **Gyroscope** *(pila max: 64)*

## ARCHEAN_hid

- `ARCHEAN_hid.PushButton` — **Push Button** *(pila max: 256)*
- `ARCHEAN_hid.ToggleButton` — **Toggle Button** *(pila max: 256)*
- `ARCHEAN_hid.ThrottleLever` — **Throttle Lever** *(pila max: 256)*
- `ARCHEAN_hid.Numpad` — **Numpad** *(pila max: 256)*
- `ARCHEAN_hid.Keyboard` — **Keyboard** *(pila max: 256)*

## ARCHEAN_hud

- `ARCHEAN_hud.HudController` — **Hud Controller** *(pila max: 256)*

## ARCHEAN_junction

- `ARCHEAN_junction.FluidJunction` — **Fluid Junction** *(pila max: 256)*
- `ARCHEAN_junction.DataJunction` — **Data Junction** *(pila max: 256)*
- `ARCHEAN_junction.LowVoltageJunction` — **Low Voltage Junction** *(pila max: 256)*
- `ARCHEAN_junction.HighVoltageJunction` — **High Voltage Junction** *(pila max: 256)*
- `ARCHEAN_junction.ItemJunction` — **Item Junction** *(pila max: 256)*
- `ARCHEAN_junction.LowVoltageRelay` — **Low Voltage Relay** *(pila max: 256)*
- `ARCHEAN_junction.HighVoltageRelay` — **High Voltage Relay** *(pila max: 256)*
- `ARCHEAN_junction.PowerConverter` — **Power Converter** *(pila max: 256)*
- `ARCHEAN_junction.DataBridge` — **Data Bridge** *(pila max: 256)*
- `ARCHEAN_junction.LowVoltageBridge` — **Low Voltage Bridge** *(pila max: 256)*
- `ARCHEAN_junction.HighVoltageBridge` — **High Voltage Bridge** *(pila max: 256)*
- `ARCHEAN_junction.FluidBridge` — **Fluid Bridge** *(pila max: 256)*
- `ARCHEAN_junction.ItemBridge` — **Item Bridge** *(pila max: 256)*
- `ARCHEAN_junction.FluidValve` — **Fluid Valve** *(pila max: 256)*

## ARCHEAN_light

- `ARCHEAN_light.SpotLight` — **Spot Light** *(pila max: 256)*
- `ARCHEAN_light.Lamp` — **Lamp** *(pila max: 256)*
- `ARCHEAN_light.SlopeLight` — **Slope Light** *(pila max: 256)*

## ARCHEAN_machines

- `ARCHEAN_machines.Crusher` — **Crusher** *(pila max: 8)*
- `ARCHEAN_machines.Crafter` — **Crafter** *(pila max: 64)*
- `ARCHEAN_machines.ItemConveyor` — **Item Conveyor** *(pila max: 64)*
- `ARCHEAN_machines.PassiveRadiator` — **Passive Radiator** *(pila max: 64)*
- `ARCHEAN_machines.ActiveRadiator` — **Active Radiator** *(pila max: 64)*
- `ARCHEAN_machines.SteamTurbine` — **Steam Turbine** *(pila max: 8)*
- `ARCHEAN_machines.SmallSteamTurbine` — **Small Steam Turbine** *(pila max: 64)*

## ARCHEAN_misc

- `ARCHEAN_misc.Step` — **Step** *(pila max: 256)*

## ARCHEAN_nuclear

- `ARCHEAN_nuclear.FissionReactor` — **Fission Reactor** *(pila max: 8)*
- `ARCHEAN_nuclear.UraniumPowder` — **Uranium Powder** *(pila max: 10000000)*
- `ARCHEAN_nuclear.FluoritePowder` — **Fluorite Powder** *(pila max: 10000000)*
- `ARCHEAN_nuclear.YellowCake` — **Yellow Cake** *(pila max: 10000000)*
- `ARCHEAN_nuclear.UraniumDioxide` — **Uranium Dioxide** *(pila max: 10000000)*
- `ARCHEAN_nuclear.PlutoniumDioxide` — **Plutonium Dioxide** *(pila max: 10000000)*
- `ARCHEAN_nuclear.UraniumTetrafluoride` — **Uranium Tetrafluoride** *(pila max: 10000000)*
- `ARCHEAN_nuclear.UranylFluoride` — **Uranyl Fluoride** *(pila max: 10000000)*
- `ARCHEAN_nuclear.UraniumRod` — **Uranium Rod** *(pila max: 1)*
- `ARCHEAN_nuclear.PlutoniumPellet` — **Plutonium Pellet** *(pila max: 1)*

## ARCHEAN_ownership

- `ARCHEAN_ownership.OwnerPad` — **Owner Pad** *(pila max: 256)*

## ARCHEAN_parts

- `ARCHEAN_parts.Screw` — **Screw** *(pila max: 1024)*
- `ARCHEAN_parts.Plastic` — **Plastic** *(pila max: 1024)*
- `ARCHEAN_parts.Wire` — **Wire** *(pila max: 1024)*
- `ARCHEAN_parts.Connector` — **Connector** *(pila max: 1024)*
- `ARCHEAN_parts.Magnet` — **Magnet** *(pila max: 256)*
- `ARCHEAN_parts.Diode` — **Diode** *(pila max: 1024)*
- `ARCHEAN_parts.Capacitor` — **Capacitor** *(pila max: 1024)*
- `ARCHEAN_parts.Panel` — **Panel** *(pila max: 256)*
- `ARCHEAN_parts.Disk` — **Disk** *(pila max: 256)*
- `ARCHEAN_parts.Casing` — **Casing** *(pila max: 256)*
- `ARCHEAN_parts.Circuit` — **Circuit** *(pila max: 1024)*
- `ARCHEAN_parts.GoldPlatedCircuit` — **Gold Plated Circuit** *(pila max: 1024)*
- `ARCHEAN_parts.Motor` — **Motor** *(pila max: 256)*
- `ARCHEAN_parts.LED` — **LED** *(pila max: 1024)*

## ARCHEAN_propeller

- `ARCHEAN_propeller.Propeller` — **Propeller** *(pila max: 64)*

## ARCHEAN_rcs

- `ARCHEAN_rcs.RCS` — **RCS** *(pila max: 256)*

## ARCHEAN_rtg

- `ARCHEAN_rtg.RTG` — **RTG** *(pila max: 256)*

## ARCHEAN_sensor1

- `ARCHEAN_sensor1.AltitudeSensor` — **Altitude Sensor** *(pila max: 256)*
- `ARCHEAN_sensor1.SpeedSensor` — **Speed Sensor** *(pila max: 256)*
- `ARCHEAN_sensor1.TiltSensor` — **Tilt Sensor** *(pila max: 256)*
- `ARCHEAN_sensor1.LinearVelocitySensor` — **Linear Velocity Sensor** *(pila max: 256)*
- `ARCHEAN_sensor1.AngularVelocitySensor` — **Angular Velocity Sensor** *(pila max: 256)*
- `ARCHEAN_sensor1.VehiclePhysicsSensor` — **Vehicle Physics Sensor** *(pila max: 256)*
- `ARCHEAN_sensor1.DensitySensor` — **Density Sensor** *(pila max: 256)*
- `ARCHEAN_sensor1.TemperatureSensor` — **Temperature Sensor** *(pila max: 256)*
- `ARCHEAN_sensor1.PlayerSensor` — **Player Sensor** *(pila max: 256)*
- `ARCHEAN_sensor1.DistanceSensor` — **Distance Sensor** *(pila max: 256)*

## ARCHEAN_storage

- `ARCHEAN_storage.TrashCan` — **Trash Can** *(pila max: 256)*
- `ARCHEAN_storage.Container` — **Container** *(pila max: 64)*
- `ARCHEAN_storage.SmallContainer` — **Small Container** *(pila max: 64)*

## ARCHEAN_tank

- `ARCHEAN_tank.SmallFluidTank` — **Small Fluid Tank** *(pila max: 256)*
- `ARCHEAN_tank.FluidTank` — **Fluid Tank** *(pila max: 64)*
- `ARCHEAN_tank.BigFluidTank` — **Big Fluid Tank** *(pila max: 8)*

## ARCHEAN_thruster

- `ARCHEAN_thruster.MiniThruster` — **Mini Thruster** *(pila max: 256)*
- `ARCHEAN_thruster.SmallThruster` — **Small Thruster** *(pila max: 256)*
- `ARCHEAN_thruster.BigThruster` — **Big Thruster** *(pila max: 8)*
- `ARCHEAN_thruster.FluidPump` — **Fluid Pump** *(pila max: 256)*
- `ARCHEAN_thruster.SmallTurboPump` — **Small Turbo Pump** *(pila max: 256)*
- `ARCHEAN_thruster.BigTurboPump` — **Big Turbo Pump** *(pila max: 64)*

## ARCHEAN_wheel

- `ARCHEAN_wheel.BigWheel` — **Big Wheel** *(pila max: 8)*
- `ARCHEAN_wheel.Wheel` — **Wheel** *(pila max: 64)*
- `ARCHEAN_wheel.SmallWheel` — **Small Wheel** *(pila max: 256)*

