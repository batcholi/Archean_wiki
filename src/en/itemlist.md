# Item & Module Names

Every item, block and component in the game, with the exact name (`<Module>.<Item>`) to use in your XenonCode programs.

> Tip: you can also get these at runtime with `get_recipes()` and `get_recipe()`.

## ARCHEAN_aileron

- `ARCHEAN_aileron.Aileron` — **Aileron** *(max stack: 256)*

## ARCHEAN_anchor

- `ARCHEAN_anchor.GroundAnchor` — **Ground Anchor** *(max stack: 256)*

## ARCHEAN_avatar

- `ARCHEAN_avatar.Canister` — **Canister** *(max stack: 256)*
- `ARCHEAN_avatar.PilotSeat` — **Pilot Seat** *(max stack: 64)*
- `ARCHEAN_avatar.Bobblehead` — **Bobblehead** *(max stack: 256)*

## ARCHEAN_battery

- `ARCHEAN_battery.LowVoltageBattery` — **Low Voltage Battery** *(max stack: 256)*
- `ARCHEAN_battery.HighVoltageBattery` — **High Voltage Battery** *(max stack: 64)*

## ARCHEAN_beacon

- `ARCHEAN_beacon.Beacon` — **Beacon** *(max stack: 256)*

## ARCHEAN_beep

- `ARCHEAN_beep.Buzzer` — **Buzzer** *(max stack: 256)*

## ARCHEAN_build

- `ARCHEAN_build.SteelBeam` — **Steel Beam** *(max stack: 10000)*
- `ARCHEAN_build.SteelRod` — **Steel Rod** *(max stack: 10000)*
- `ARCHEAN_build.ConcreteBlock` — **Concrete Block** *(max stack: 10000)*
- `ARCHEAN_build.CompositeBlock` — **Composite Block** *(max stack: 10000)*
- `ARCHEAN_build.SteelBlock` — **Steel Block** *(max stack: 10000)*
- `ARCHEAN_build.AluminiumBlock` — **Aluminium Block** *(max stack: 10000)*
- `ARCHEAN_build.GlassBlock` — **Glass Block** *(max stack: 10000)*
- `ARCHEAN_build.LeadBlock` — **Lead Block** *(max stack: 256)*
- `ARCHEAN_build.ConstructorTool` — **Constructor Tool** *(max stack: 1)*
- `ARCHEAN_build.DestructorTool` — **Destructor Tool** *(max stack: 1)*
- `ARCHEAN_build.BlueprintTool` — **Blueprint Tool** *(max stack: 1)*
- `ARCHEAN_build.PaintTool` — **Paint Tool** *(max stack: 1)*
- `ARCHEAN_build.LabelTool` — **Label Tool** *(max stack: 1)*
- `ARCHEAN_build.DashboardTool` — **Dashboard Tool** *(max stack: 1)*
- `ARCHEAN_build.SpoolTool` — **Spool Tool** *(max stack: 1)*
- `ARCHEAN_build.DataCable` — **Data Cable** *(max stack: 10000000)*
- `ARCHEAN_build.LowVoltageCable` — **Low Voltage Cable** *(max stack: 10000000)*
- `ARCHEAN_build.HighVoltageCable` — **High Voltage Cable** *(max stack: 10000000)*
- `ARCHEAN_build.FluidPipe` — **Fluid Pipe** *(max stack: 10000000)*
- `ARCHEAN_build.ItemConduit` — **Item Conduit** *(max stack: 10000000)*
- `ARCHEAN_build.SmallPivot` — **Small Pivot** *(max stack: 256)*
- `ARCHEAN_build.SmallHinge` — **Small Hinge** *(max stack: 256)*
- `ARCHEAN_build.LinearTrack` — **Linear Track** *(max stack: 256)*
- `ARCHEAN_build.DockableDoor` — **Dockable Door** *(max stack: 8)*
- `ARCHEAN_build.DockingPort` — **Docking Port** *(max stack: 64)*
- `ARCHEAN_build.Volume` — **Volume** *(max stack: 256)*
- `ARCHEAN_build.Hitch` — **Hitch** *(max stack: 64)*

## ARCHEAN_celestial

- `ARCHEAN_celestial.Aluminium` — **Aluminium** *(max stack: 10000000)*
- `ARCHEAN_celestial.Chrome` — **Chrome** *(max stack: 10000000)*
- `ARCHEAN_celestial.Copper` — **Copper** *(max stack: 10000000)*
- `ARCHEAN_celestial.Gold` — **Gold** *(max stack: 10000000)*
- `ARCHEAN_celestial.Iron` — **Iron** *(max stack: 10000000)*
- `ARCHEAN_celestial.Lead` — **Lead** *(max stack: 10000000)*
- `ARCHEAN_celestial.Nickel` — **Nickel** *(max stack: 10000000)*
- `ARCHEAN_celestial.Silicon` — **Silicon** *(max stack: 10000000)*
- `ARCHEAN_celestial.Silver` — **Silver** *(max stack: 10000000)*
- `ARCHEAN_celestial.Tin` — **Tin** *(max stack: 10000000)*
- `ARCHEAN_celestial.Titanium` — **Titanium** *(max stack: 10000000)*
- `ARCHEAN_celestial.Tungsten` — **Tungsten** *(max stack: 10000000)*
- `ARCHEAN_celestial.Uranium` — **Uranium** *(max stack: 10000000)*
- `ARCHEAN_celestial.Carbon` — **Carbon** *(max stack: 10000000)*
- `ARCHEAN_celestial.Sulfur` — **Sulfur** *(max stack: 10000000)*
- `ARCHEAN_celestial.Fluorite` — **Fluorite** *(max stack: 10000000)*
- `ARCHEAN_celestial.Rock` — **Rock** *(max stack: 10000)*
- `ARCHEAN_celestial.RockRakeTool` — **Rock Rake Tool** *(max stack: 1)*
- `ARCHEAN_celestial.OreScannerTool` — **Ore Scanner Tool** *(max stack: 1)*
- `ARCHEAN_celestial.NavInstrument` — **Nav Instrument** *(max stack: 256)*
- `ARCHEAN_celestial.MiniNavInstrument` — **Mini Nav Instrument** *(max stack: 256)*
- `ARCHEAN_celestial.SolarPanel` — **Solar Panel** *(max stack: 256)*
- `ARCHEAN_celestial.SolarSensor` — **Solar Sensor** *(max stack: 256)*
- `ARCHEAN_celestial.MiningDrill` — **Mining Drill** *(max stack: 8)*
- `ARCHEAN_celestial.OreScanner` — **Ore Scanner** *(max stack: 256)*
- `ARCHEAN_celestial.TerrainScanner` — **Terrain Scanner** *(max stack: 256)*
- `ARCHEAN_celestial.GeothermalExchanger` — **Geothermal Exchanger** *(max stack: 64)*

## ARCHEAN_chemical

- `ARCHEAN_chemical.FluidPort` — **Fluid Port** *(max stack: 256)*
- `ARCHEAN_chemical.Electrolyser` — **Electrolyser** *(max stack: 8)*
- `ARCHEAN_chemical.SabatierReactor` — **Sabatier Reactor** *(max stack: 64)*
- `ARCHEAN_chemical.EnrichmentCentrifuge` — **Enrichment Centrifuge** *(max stack: 64)*
- `ARCHEAN_chemical.ChemicalFurnace` — **Chemical Furnace** *(max stack: 8)*

## ARCHEAN_computer

- `ARCHEAN_computer.Computer` — **Computer** *(max stack: 256)*
- `ARCHEAN_computer.MiniComputer` — **Mini Computer** *(max stack: 256)*
- `ARCHEAN_computer.Router` — **Router** *(max stack: 256)*
- `ARCHEAN_computer.MiniRouter` — **Mini Router** *(max stack: 256)*
- `ARCHEAN_computer.HDD` — **HDD** *(max stack: 256)*

## ARCHEAN_gyroscope

- `ARCHEAN_gyroscope.SmallGyroscope` — **Small Gyroscope** *(max stack: 64)*
- `ARCHEAN_gyroscope.Gyroscope` — **Gyroscope** *(max stack: 64)*

## ARCHEAN_hid

- `ARCHEAN_hid.PushButton` — **Push Button** *(max stack: 256)*
- `ARCHEAN_hid.ToggleButton` — **Toggle Button** *(max stack: 256)*
- `ARCHEAN_hid.ThrottleLever` — **Throttle Lever** *(max stack: 256)*
- `ARCHEAN_hid.Numpad` — **Numpad** *(max stack: 256)*
- `ARCHEAN_hid.Keyboard` — **Keyboard** *(max stack: 256)*

## ARCHEAN_hud

- `ARCHEAN_hud.HudController` — **Hud Controller** *(max stack: 256)*

## ARCHEAN_junction

- `ARCHEAN_junction.FluidJunction` — **Fluid Junction** *(max stack: 256)*
- `ARCHEAN_junction.DataJunction` — **Data Junction** *(max stack: 256)*
- `ARCHEAN_junction.LowVoltageJunction` — **Low Voltage Junction** *(max stack: 256)*
- `ARCHEAN_junction.HighVoltageJunction` — **High Voltage Junction** *(max stack: 256)*
- `ARCHEAN_junction.ItemJunction` — **Item Junction** *(max stack: 256)*
- `ARCHEAN_junction.LowVoltageRelay` — **Low Voltage Relay** *(max stack: 256)*
- `ARCHEAN_junction.HighVoltageRelay` — **High Voltage Relay** *(max stack: 256)*
- `ARCHEAN_junction.PowerConverter` — **Power Converter** *(max stack: 256)*
- `ARCHEAN_junction.DataBridge` — **Data Bridge** *(max stack: 256)*
- `ARCHEAN_junction.LowVoltageBridge` — **Low Voltage Bridge** *(max stack: 256)*
- `ARCHEAN_junction.HighVoltageBridge` — **High Voltage Bridge** *(max stack: 256)*
- `ARCHEAN_junction.FluidBridge` — **Fluid Bridge** *(max stack: 256)*
- `ARCHEAN_junction.ItemBridge` — **Item Bridge** *(max stack: 256)*
- `ARCHEAN_junction.FluidValve` — **Fluid Valve** *(max stack: 256)*

## ARCHEAN_light

- `ARCHEAN_light.SpotLight` — **Spot Light** *(max stack: 256)*
- `ARCHEAN_light.Lamp` — **Lamp** *(max stack: 256)*
- `ARCHEAN_light.SlopeLight` — **Slope Light** *(max stack: 256)*

## ARCHEAN_machines

- `ARCHEAN_machines.Crusher` — **Crusher** *(max stack: 8)*
- `ARCHEAN_machines.Crafter` — **Crafter** *(max stack: 64)*
- `ARCHEAN_machines.ItemConveyor` — **Item Conveyor** *(max stack: 64)*
- `ARCHEAN_machines.PassiveRadiator` — **Passive Radiator** *(max stack: 64)*
- `ARCHEAN_machines.ActiveRadiator` — **Active Radiator** *(max stack: 64)*
- `ARCHEAN_machines.SteamTurbine` — **Steam Turbine** *(max stack: 8)*
- `ARCHEAN_machines.SmallSteamTurbine` — **Small Steam Turbine** *(max stack: 64)*

## ARCHEAN_misc

- `ARCHEAN_misc.Step` — **Step** *(max stack: 256)*

## ARCHEAN_nuclear

- `ARCHEAN_nuclear.FissionReactor` — **Fission Reactor** *(max stack: 8)*
- `ARCHEAN_nuclear.UraniumPowder` — **Uranium Powder** *(max stack: 10000000)*
- `ARCHEAN_nuclear.FluoritePowder` — **Fluorite Powder** *(max stack: 10000000)*
- `ARCHEAN_nuclear.YellowCake` — **Yellow Cake** *(max stack: 10000000)*
- `ARCHEAN_nuclear.UraniumDioxide` — **Uranium Dioxide** *(max stack: 10000000)*
- `ARCHEAN_nuclear.PlutoniumDioxide` — **Plutonium Dioxide** *(max stack: 10000000)*
- `ARCHEAN_nuclear.UraniumTetrafluoride` — **Uranium Tetrafluoride** *(max stack: 10000000)*
- `ARCHEAN_nuclear.UranylFluoride` — **Uranyl Fluoride** *(max stack: 10000000)*
- `ARCHEAN_nuclear.UraniumRod` — **Uranium Rod** *(max stack: 1)*
- `ARCHEAN_nuclear.PlutoniumPellet` — **Plutonium Pellet** *(max stack: 1)*

## ARCHEAN_ownership

- `ARCHEAN_ownership.OwnerPad` — **Owner Pad** *(max stack: 256)*

## ARCHEAN_parts

- `ARCHEAN_parts.Screw` — **Screw** *(max stack: 1024)*
- `ARCHEAN_parts.Plastic` — **Plastic** *(max stack: 1024)*
- `ARCHEAN_parts.Wire` — **Wire** *(max stack: 1024)*
- `ARCHEAN_parts.Connector` — **Connector** *(max stack: 1024)*
- `ARCHEAN_parts.Magnet` — **Magnet** *(max stack: 256)*
- `ARCHEAN_parts.Diode` — **Diode** *(max stack: 1024)*
- `ARCHEAN_parts.Capacitor` — **Capacitor** *(max stack: 1024)*
- `ARCHEAN_parts.Panel` — **Panel** *(max stack: 256)*
- `ARCHEAN_parts.Disk` — **Disk** *(max stack: 256)*
- `ARCHEAN_parts.Casing` — **Casing** *(max stack: 256)*
- `ARCHEAN_parts.Circuit` — **Circuit** *(max stack: 1024)*
- `ARCHEAN_parts.GoldPlatedCircuit` — **Gold Plated Circuit** *(max stack: 1024)*
- `ARCHEAN_parts.Motor` — **Motor** *(max stack: 256)*
- `ARCHEAN_parts.LED` — **LED** *(max stack: 1024)*

## ARCHEAN_propeller

- `ARCHEAN_propeller.Propeller` — **Propeller** *(max stack: 64)*

## ARCHEAN_rcs

- `ARCHEAN_rcs.RCS` — **RCS** *(max stack: 256)*

## ARCHEAN_rtg

- `ARCHEAN_rtg.RTG` — **RTG** *(max stack: 256)*

## ARCHEAN_sensor1

- `ARCHEAN_sensor1.AltitudeSensor` — **Altitude Sensor** *(max stack: 256)*
- `ARCHEAN_sensor1.SpeedSensor` — **Speed Sensor** *(max stack: 256)*
- `ARCHEAN_sensor1.TiltSensor` — **Tilt Sensor** *(max stack: 256)*
- `ARCHEAN_sensor1.LinearVelocitySensor` — **Linear Velocity Sensor** *(max stack: 256)*
- `ARCHEAN_sensor1.AngularVelocitySensor` — **Angular Velocity Sensor** *(max stack: 256)*
- `ARCHEAN_sensor1.VehiclePhysicsSensor` — **Vehicle Physics Sensor** *(max stack: 256)*
- `ARCHEAN_sensor1.DensitySensor` — **Density Sensor** *(max stack: 256)*
- `ARCHEAN_sensor1.TemperatureSensor` — **Temperature Sensor** *(max stack: 256)*
- `ARCHEAN_sensor1.PlayerSensor` — **Player Sensor** *(max stack: 256)*
- `ARCHEAN_sensor1.DistanceSensor` — **Distance Sensor** *(max stack: 256)*

## ARCHEAN_storage

- `ARCHEAN_storage.TrashCan` — **Trash Can** *(max stack: 256)*
- `ARCHEAN_storage.Container` — **Container** *(max stack: 64)*
- `ARCHEAN_storage.SmallContainer` — **Small Container** *(max stack: 64)*

## ARCHEAN_tank

- `ARCHEAN_tank.SmallFluidTank` — **Small Fluid Tank** *(max stack: 256)*
- `ARCHEAN_tank.FluidTank` — **Fluid Tank** *(max stack: 64)*
- `ARCHEAN_tank.BigFluidTank` — **Big Fluid Tank** *(max stack: 8)*

## ARCHEAN_thruster

- `ARCHEAN_thruster.MiniThruster` — **Mini Thruster** *(max stack: 256)*
- `ARCHEAN_thruster.SmallThruster` — **Small Thruster** *(max stack: 256)*
- `ARCHEAN_thruster.BigThruster` — **Big Thruster** *(max stack: 8)*
- `ARCHEAN_thruster.FluidPump` — **Fluid Pump** *(max stack: 256)*
- `ARCHEAN_thruster.SmallTurboPump` — **Small Turbo Pump** *(max stack: 256)*
- `ARCHEAN_thruster.BigTurboPump` — **Big Turbo Pump** *(max stack: 64)*

## ARCHEAN_wheel

- `ARCHEAN_wheel.BigWheel` — **Big Wheel** *(max stack: 8)*
- `ARCHEAN_wheel.Wheel` — **Wheel** *(max stack: 64)*
- `ARCHEAN_wheel.SmallWheel` — **Small Wheel** *(max stack: 256)*

